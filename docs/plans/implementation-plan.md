# Forge MVP implementation plan

**Updated:** 2026-08-17
**Status:** active

## Rules

- Complete a stage only with reproducible evidence and a green repository gate.
- Update normative ADRs and versioned contracts before implementation.
- Keep one command model, one project snapshot, one sprint journal, and one review engine.
- Keep every project cross-platform unless it is a marked minimal leaf OS adapter under ADR 0007.
- Keep provider-neutral policy in the core and every vendor/OS detail in its provider-owned adapter under ADR 0008.
- Apply the implementation-first testing invariant to Forge development and every managed project; generated integrations communicate it and Host transitions enforce it.
- Defer work that has no MVP acceptance case or measured need.

## Progress

- [x] Stages 0–7 — Foundation, solution, updater, startup, providers, durable workflow, Git isolation, and routing.
- [x] Stage 8 — Cross-platform Host, local control plane, and modular providers.
- [x] Stage 9 — `.forge/` compiler and agent integration.
- [x] Stage 10 — Reproducible context assembly.
- [ ] Stage 11 — `implementation-critical` workflow and UI parity.
- [ ] Stage 12 — Diagnostics, evaluations, and hardening.
- [ ] Stage 13 — MVP release and acceptance.

## Completed foundation

### Stage 0 — Contracts and boundaries

- [x] Freeze the Windows MVP distribution, cross-platform core, `.forge/` source of truth, trust boundaries, stable-release policy, state machines, capabilities, localization, configuration scopes, and recovery rules.

### Stage 1 — Solution skeleton

- [x] Create the .NET solution, Runtime, CLI, Desktop, neutral updater, Windows updater adapter, dependency pinning, formatting, analyzers, architecture tests, and CI gate.

### Stage 2 — Neutral self-updater

- [x] Implement release lookup, SemVer selection, target detection, checksum verification, staging, activation contract, restart handshake, and rollback orchestration without an OS dependency.

### Stage 3 — Windows update adapter

- [x] Implement per-user Windows installation, immutable versions, PATH/shortcut integration, restart coordination, x64/Arm64 publishing, rollback, and release tests.

### Stage 4 — Startup and project initialization

- [x] Implement fail-closed startup, scoped configuration, localization, explicit project-root initialization, atomic `.forge/` publication, status snapshot, suggestions, and both initial surfaces.

### Stage 5 — Provider toolchain proof

- [x] Implement read-only discovery, explicit vendor-native install/update, bounded version probes, absolute process invocation, Codex/Claude JSON normalization, redaction, and stable failures.

### Stage 6 — Durable workflow

- [x] Implement immutable sprint definitions, append-only workflow journal, optimistic concurrency, idempotency, crash recovery, DAG scheduling, attempts, human gates, findings, handoffs, results, retries, and cross-sprint isolation.

### Stage 7 — Git isolation and fallback

- [x] Implement integration/attempt worktrees, fast-forward integration, gated rebase, clean replay, cleanup recovery, fixed sprint retry budget, and provider/model/surface circuit breakers.
- [x] Simplify persistence: discover sprints from journal creation markers, omit runtime sprint ids from the manifest, record routing in the sprint journal, fold budget/breakers from it, and migrate legacy routing sidecars idempotently.

## Stage 8 — Cross-platform Host, local control plane, and modular providers

**Depends on:** Stages 4–7.

- [x] P8.1–P8.8 — Enforce ADR 0007: mark OS adapters; make CLI/shared tests portable; move Windows composition and directory-flush interop to minimal adapters; split reusable Desktop state from WinUI; audit the Windows updater adapter.
- [x] P8.9–P8.17 — Add portable `Forge.Host` and client SDK as the only workflow writer; implement discovery, start, reconnect, message limits, deadlines, correlation, version/capability handshake, and stable diagnostics. **Reopened 2026-08-15 audit:** the Host protocol only dispatches `ping`/`GetProjectSnapshot`/`ReadControlEvents` (all read-only) — it is not yet the only workflow writer. `ForgeHostClient` does not validate the handshake's echoed correlation id/version, and the Host ignores the client's capability list. **Closed 2026-08-16:** the Host now dispatches `recover_startup`/`set_configuration` too, and both the CLI (`forge doctor --recover`, `forge config project`) and Desktop route both through the project's Host via `IForgeMutations`/`RemoteForgeMutations`/`HostMutationsFactory`, starting one if none is running. `ForgeHostClient` now independently validates the handshake response's echoed correlation id and protocol version (never trusting the Host's own self-reported diagnostic alone), and the Host's handshake response now advertises its real capability set (`CapabilityIds.Implemented`) instead of a hardcoded empty list.
- [x] P8.18–P8.24 — Use one asynchronous `System.IO.Pipes` transport and one named `Mutex` project lease through cross-platform BCL APIs; add no OS branch, TCP fallback, or transport package.
- [x] P8.25–P8.33 — Implement `GetProjectSnapshot(detail, sprint_id?)` and `ReadControlEvents`. Project summary, next action, tree, sprint inspection, provider/integration status, CLI output, and Desktop views must be local projections of the snapshot. **Reopened 2026-08-15 audit:** snapshot DTOs omit integration status, phase profile, and active deadline; attempt `LastActivityAt` is dropped during projection; CLI has no `tree`/`sprint inspect` commands; Desktop shows only a general overview, not per-sprint detail. **Progress 2026-08-16:** `LastActivityAt` now survives projection into `EntityStatus` (snapshot contract 1.2.0 → 1.3.0); CLI gained `forge tree` (nests attempts under their owning node via `OwnerId`, something the flat `status` lists never showed) and `forge sprint inspect <id>` (a dedicated single-sprint entry point, matching ADR 0005's own naming), both local projections of the existing snapshot with no new backend query. **Closed 2026-08-17:** Desktop gained the sprint tree and per-sprint detail view, both local projections of the same `GetProjectSnapshot` result the CLI reads; the tree/detail rendering now lives in `SurfaceFormatting`, and `SurfaceParityTests.DesktopAndCliRenderTheSameSprintTreeAndDetailForOneSnapshot` asserts the two surfaces produce identical text for one project rather than merely calling one helper. Integration status, phase profile, and active deadline are deliberately **not** part of this item's completion: no executor exists anywhere that would populate them with real data (`SprintGitIsolation` is unwired from `SprintOrchestrator`/`SprintScheduler`; nothing in production code constructs an `ExecutionProfile`), so projecting them now would only surface permanently empty fields. They are re-scoped to Stage 11 P11.56–P11.66, which owns the surfaces that first produce them.
- [x] P8.34–P8.41 — Isolate release, Debug, and tests by instance id; test same-user enforcement, hostile clients, crashes, abandoned leases, stale clients, recovery, and three-OS behavior. **Reopened 2026-08-15 audit:** same-user enforcement (`CurrentUserOnly`) has no automated test with a second OS user. **Progress 2026-08-16:** added `.github/scripts/test-same-user-isolation.ps1`, a Windows CI job that creates a genuine second local OS user and proves the real production `NamedPipeControlTransport` (`PipeOptions.CurrentUserOnly`) blocks its connection attempt to the control-plane pipe — with a same-user sanity check and a positive control first, and asserting the specific access-denial exception, so the isolation result can't be a false positive. `MutexProjectLease` (a separate `NamedWaitHandleOptions.CurrentUserOnly` primitive, not a pipe) was left uncovered by that check. **Closed 2026-08-16:** added a new `Forge.MutexIsolationProbe` project exercising the real `MutexProjectLease.TryAcquire`, and extended `test-same-user-isolation.ps1` to run in the same CI job/local-user lifecycle, proving same-user mutual exclusion first (a second same-user acquire attempt on an identical name blocks and times out while the first holder is alive), then that a *different* local OS user is denied access to a mutex of that identical name the current user already holds (`CurrentUserOnly` restricts a security descriptor on one shared object; it does not namespace per user). Getting this check to actually pass surfaced and fixed a real, separate defect: both `MutexProjectLease` and `ProviderInstallLock` always constructed their mutex in the OS-wide `Global\` namespace, which Windows only lets administrators/service accounts create (`SeCreateGlobalPrivilege`) — a standard non-admin user's Host failed outright at startup. Two intermediate fixes were tried and rejected before the final one. First, determining once from a dedicated always-uncontended probe whether the account can use `Global\`: the probe reflects the *process token*, not the account, so the same admin user's elevated and non-elevated Forge processes could resolve to different namespaces and silently stop excluding each other — a common scenario, not a rare edge case, and a fail-open regression from the lease's whole purpose. Second, uniform `CurrentSessionOnly = true` for every process on every OS: this fixed the token problem but unnecessarily weakened Unix too, where a session is only a single shell and `Global\`'s equivalent never had a privilege problem to begin with. Both types now fix `CurrentSessionOnly` per platform — `true` on Windows, `false` everywhere else — via `OperatingSystem.IsWindows()`, keeping the strongest guarantee wherever the OS actually allows it; a static architecture test pins the two independent copies of this construction identical. Known trade-off, disclosed in ADR 0005 and `CHANGELOG.md`: Windows session-scoping does not extend across two concurrent sessions of the same user (e.g. console + a simultaneous RDP session). Abandoned-lease recovery, crash resilience, and idempotent transitions were already covered by `ForgeHostProcessTests.ASuccessorHostRecoversTheProjectLeaseAfterTheFirstHostCrashes` and `ProjectLeaseTests`; three-OS portability is enforced by `test-portable.ps1` building both probe projects on every OS.
- [x] P8.42–P8.47 — Add Host-owned `resume_not_before` scheduling, safe activity updates, notification projection, and idempotent timer recovery without sleeping executor slots. `RoutingLedger` computes and records `resume_not_before`; `RoutingStatus.ResumeNotBefore` already projects it into the snapshot; `AttemptActivityRecordedType` already provides safe throttled activity updates. **Reopened 2026-08-15 audit:** nothing wakes and re-enqueues a deferred attempt once `resume_not_before` elapses. **Progress 2026-08-16:** added `ResumeSchedulerHostedService`, a Host-owned periodic `BackgroundService` (started only after the Host wins the project lease, per ADR 0005) that re-derives every sprint's `pending`→`ready` node readiness via the existing idempotent `SprintScheduler.AdvanceGraphAsync` — no per-sprint memory, safe to call repeatedly, restart-safe. **Closed 2026-08-17:** acting on `RouteDecision.Outcome.Deferred`/`resume_not_before` directly means starting a fresh attempt once a routing deferral elapses, which requires an attempt executor. No such executor exists anywhere in the repo today (`SprintScheduler.StartAttemptAsync` and `ILlmProvider.RunAsync` have no production caller — confirmed by repo-wide search), and building one here would duplicate or stub Stage 11's own engine rather than complete Stage 8's Host/control-plane scope. This item is deliberately **not** re-attempted within Stage 8: every timer/recovery/activity/notification-projection surface buildable without an execution engine now exists (`ResumeSchedulerHostedService`, `AttemptActivityRecordedType`, `RoutingStatus.ResumeNotBefore`), and the remaining "wake a deferred attempt" semantics are already Stage 11's territory — P11.32–P11.40 (the attempt-execution engine itself) and P11.48–P11.55 ("durable rate-limit deferral ... with confirmation, version, idempotency, bounded instruction, cancellation, worktree discard, linkage, and clean replacement"), which is precisely `RouteDecision.Outcome.Deferred` resumption. This mirrors how P8.25–P8.33 re-scoped its own executor-dependent fields to Stage 11.
- [x] P8.48–P8.54 — Version the user-configuration, provider-health, startup-check, snapshot, diagnostic, and execution-profile contracts for ordered provider enablement, update/authentication state, lineage evidence, compatibility, and migration.
- [x] P8.55–P8.63 — Reduce the core to `ILlmProvider`, `ProviderId`, and generic lifecycle/routing policy. Move all Codex details to `Forge.Providers.Codex.Windows` and all Claude Code details to `Forge.Providers.Claude.Windows`; remove concrete provider registrations and any shared `Forge.Providers.Windows` concept from neutral code.
- [x] P8.64–P8.71 — Register built-ins explicitly with `AddCodexProvider()` and `AddClaudeProvider()`; validate unique ids; resolve omitted, empty, ordered, duplicate, and unknown `providers.enabled` values; intersect and freeze project provider constraints without probing disabled providers. **Reopened 2026-08-15 audit:** enablement resolution was done, but nothing computed the project execution profile's provider intersection or froze it into `SprintDefinition`. **Closed 2026-08-16:** `SprintOrchestrator.CreateSprintAsync` now resolves `ProviderCatalog.ResolveEnabled` against the current enabled-provider ids and freezes the ordered result into `SprintDefinition.FrozenProviders`, once, same as every other frozen-at-creation field; an empty result fails creation with `sprint_provider_candidates_empty` instead of registering a sprint that could never route an attempt (ADR 0008). No project-scope provider constraint is configurable yet (only the user-enabled set exists), so today's intersection is that set alone — `FrozenProviders` is the exact ordered-intersection field ADR 0008 describes, ready for a project constraint to narrow once one exists. `ExecutionProfileContracts.cs` remains Stage 11's separate per-phase provider+model contract, unrelated to this ordered-candidate-list concept.
- [x] P8.72–P8.82 — Implement enabled-provider startup: bounded local probe, cached release lookup, conditional vendor update, install/repair, per-user interprocess lock, post-update probe, and final authentication check. Use 24-hour success and one-hour failure retry windows; make refresh bypass the cache but not the availability comparison; disable Claude's background auto-updater during normal execution. **Reopened 2026-08-15 audit:** `StartupPipeline` only called the read-only `CheckAsync`; install/repair/conditional update only ran through the explicit `forge models --refresh` path, never automatically at startup as this item's own heading requires. **Closed 2026-08-16:** all the per-provider machinery (bounded probe, throttled release lookup, conditional update vs. full install/repair, interprocess lock, post-update probe) already existed in `ProviderInstallation.InstallOrUpdateAsync`, but it always bypassed the release-check cache — the only caller was `forge models --refresh`, which is supposed to be the one path that bypasses it. `InstallOrUpdateAsync`, `ILlmProvider.InstallOrUpdateAsync`, and `IProviderToolchainManager.EnsureReadyAsync` now take an explicit `bypassReleaseCache` flag; `StartupPipeline.CheckProvidersAsync` calls `EnsureReadyAsync(bypassReleaseCache: false, ...)` so routine startup performs real install/repair/update while still honoring the 24h/1h cache windows, and `forge models --refresh` calls it with `true` exactly as before. Claude's `DISABLE_AUTOUPDATER=1` handling was already correct from earlier work (P8.72-82's own prior partial completion) and is unchanged.
- [x] P8.83–P8.88 — Project normalized registered/enabled/disabled/version/update/authentication/readiness states through startup, `forge models`, snapshot, CLI, and Desktop. Keep `forge models` read-only and refresh limited to enabled providers; never persist raw authentication output or identity.

**Gate:** neutral projects pass on Windows, Linux, and macOS; adapter tests pass on their OS; one Host owns mutations; active work survives client loss; both surfaces read the same snapshot/events; architecture checks reject platform leakage, concrete providers in the core, cross-provider dependencies, a shared Windows provider library, and alternate read models. Disabled providers cause no lifecycle side effects; enabled providers update only when needed and must pass explicit authentication readiness.

## Stage 9 — `.forge/` compiler and agent integration

- [x] P9.1–P9.8 — Parse manifest/YAML/Markdown/frontmatter into validated semantic input with safe paths, references, scopes, and context limits. **Closed 2026-08-17:** ADR 0009 defines the `.forge/rules/*.md` and `.forge/knowledge/*.md` document format (Markdown body with a `---`-delimited YAML frontmatter block, matching Jekyll/Hugo/Obsidian convention) and adds `forge-document.schema.json` (Draft 2020-12, `schema_version`/`id`/`title`/`scope` required, optional `references`/`context_limit_tokens`). `Forge.Compiler.ForgeDocumentCompiler` discovers both flat, optional directories and validates each document: frontmatter against the schema (typed YamlDotNet deserialization, not the raw-object path — dynamic deserialization was found to stringify every scalar, which would have silently passed integers like `context_limit_tokens` through as JSON strings and failed the schema's `"type": "integer"`); `references` against a closed safe-path check (relative, no `..`/absolute/drive/backslash, resolved and re-resolved through any symlink target to stay inside `.forge/`, and required to name another document this same parse pass discovered — not an arbitrary project path); and each document's estimated token count (chars/4 heuristic) against its effective context limit (declared value or a 4,000-token MVP default, schema-bounded at 8,000). Parsing never throws for expected content problems — malformed frontmatter, unsafe references, oversized documents, and duplicate ids are collected as typed `ForgeDocumentError` entries so one bad file never blocks the rest of the tree. Kind (`rule`/`knowledge`) is derived from directory, never a frontmatter field. Sprint-scoped specifications/decisions and structured handoffs (context-assembly layers 2–3's other half) are explicitly deferred to Stage 11, which first produces them; only rules and project knowledge/ADRs have an MVP source today.
- [x] P9.9–P9.16 — Generate reproducible native outputs for configured providers with source hashes, generator versions, drift detection, validation, and artifact-language metadata. **Closed 2026-08-17:** ADR 0010 adds `Forge.Compiler.IntegrationSourceCompiler`, which compiles a `ForgeDocumentSet` (ADR 0009) plus the project's artifact-language policy into one provider-agnostic `CanonicalIntegrationSource` — a merged, localized Markdown body whose `SourceDigest` is a pure function of canonical `.forge/` content (never a timestamp or the generator's own version), so comparing digests across generations *is* drift detection with no separate mechanism. Generation is refused with a typed diagnostic, never silently degraded, when the resolved `artifacts.language.agent_facing` isn't in `ILocalizationCatalog.SupportedCultures`. Mirroring ADR 0008's `ILlmProvider` pattern exactly, a new `IProviderIntegrationGenerator` interface (`Forge.Providers`) is implemented once per vendor in that vendor's own adapter project — `ClaudeIntegrationGenerator` (`CLAUDE.md`, a thin `@AGENTS.md` import, matching this repository's own convention) and `CodexIntegrationGenerator` (`AGENTS.md`, the full canonical body) — registered through the existing `AddClaudeProvider()`/`AddCodexProvider()` composition roots; the core and `Forge.Compiler` never name a vendor file format. `docs/contracts/v1/schemas/generated-artifact.schema.json` versions the produced metadata (cloning `handoff.schema.json`'s existing artifact shape); `Forge.Application.IntegrationGenerationService` orchestrates document parsing, source compilation, and every enabled provider's generator, validating each artifact against that schema before returning it. A JSON/frontmatter "skill" package format was considered and rejected for the MVP — neither vendor's packaging schema is pinned by any Forge contract, and the root-level instructions file is a stable, already-observed mechanism for both vendors. This item is generation only, entirely in-memory; writing an artifact into its vendor's real config location, detecting an existing installation, and removal remain P9.17-P9.24.
- [x] P9.17–P9.24 — Generate, inspect, install, and remove one canonical Forge integration for each enabled provider; document snapshot/events/commands, recovery, and built-in workflow invariants, including implementation confirmation before new test selection or authoring; exclude human authority; never overwrite unknown files. **Closed 2026-08-17:** ADR 0011 adds `forge integration skill generate|install|remove`, matching `capabilities.json`'s existing `integration.skill` contract exactly. `generate` is a plain read (never routed through the Host) that inspects every enabled provider's target file and reports missing/current/stale/foreign — satisfying "inspect" without a separate verb. `install`/`remove` are `IForgeMutations` writes, routed through `RemoteForgeMutations` → `ControlPlaneHostedService` exactly like `recover_startup`, since integration state is re-derived fresh every call (no `ExpectedStateVersion`/`IdempotencyKey`, matching `RecoverStartupAsync`'s own pattern, not `InitializeProjectAsync`'s). Ownership is decided solely by `IntegrationSourceCompiler.TryParseSourceDigest` parsing the marker back out (sharing its field-parsing logic with the writer, so the two can never drift the way a hand-rolled copy briefly did during ADR 0010's review) — a file without a recognizable marker is foreign, never overwritten or deleted, and reported via a distinct `integration_partially_refused` diagnostic (ADR 0005's "duplicate installations are detected rather than left ambiguous"). Independent review found the first version of this check insufficient: the marker's `source_digest` alone couldn't detect a hand-edit below an otherwise-intact marker line when `.forge/` itself hadn't changed, and a symlinked target path was read/written/deleted through rather than refused (including a POSIX-specific gap where `File.Exists` reports `false` for a dangling symlink, which would have let it fall through to "missing" and be silently replaced). Both are now closed: the marker embeds a second `content_digest` field — a digest of the file's own actual body, re-verified against its current content on every read — and any symlink at the target path (dangling or not) is unconditionally foreign, checked before `File.Exists`. Manually verified end-to-end through the real CLI/Host pipeline: missing→written, current→unchanged (idempotent), a rule edit→stale→regenerated, and a hand-written file→foreign→refused-and-untouched, for both `install` and `remove`; the hand-edit-below-marker and symlink cases are covered by regression tests rather than manual verification. Two things ADR 0011 explicitly defers rather than silently omits: `AgentIntegrationChanged` event emission (no project-level, non-sprint event journal exists to record it in) and the Desktop `Settings/AgentIntegration` view `capabilities.json` also names (CLI-first, matching how Stage 8's tree/detail work also landed CLI-then-Desktop across separate PRs) — both remain open follow-up work, not gaps this item silently leaves unrecorded.

**Gate:** `.forge/` remains canonical; generated files are reproducible and owned; every provider view carries the same implementation-first testing order; agents cannot invoke human-only commands.

## Stage 10 — Reproducible context assembly

- [x] P10.1–P10.8 — Build a versioned context manifest from rules, sprint specifications/decisions, accepted ADRs, structured handoffs, exact Git/file/`rg` reads, and a token budget. **Closed 2026-08-17:** ADR 0012 adds `Forge.Compiler.ContextManifestCompiler`, which builds one `ContextManifest` (`context-manifest.schema.json`) per sprint from a `ForgeDocumentSet` (ADR 0009) plus the sprint's own already-frozen id/base commit/workflow/workflow version — no new source of that identity is invented. Layer 1 (rules) and layer 3's project-knowledge/accepted-ADR half admit real MVP content; layer 2 (sprint specifications/decisions) and layer 3's structured-handoff half stay explicitly, permanently empty in the manifest shape, the same "modeled shape, no producer yet" gap ADR 0009 already named for `Handoff` — confirmed again here by grep: `Handoff` still has zero real construction sites. `forge-document.schema.json` gains one additive optional `status` field (`accepted`/`proposed`/`rejected`/`superseded`) so a `.forge/knowledge/*.md` document can represent an ADR without a second directory or a frontmatter `kind` override; only an accepted-or-unstatused knowledge document is admitted.
- [x] P10.9–P10.12 — Record source commit, selected paths, digests, truncation, and retrieval rationale; rebuild the same context without a transcript. **Closed 2026-08-17:** every admitted manifest item records its relative path, a `sha256:` content digest, its estimated token cost, and a rationale (`rule`, `knowledge:accepted`, or `query_result`); every dropped item is recorded in `truncated` with its token cost and an `over_budget` reason instead of silently vanishing. `ManifestDigest` is a pure function of every other field (never a timestamp or generator version, matching `IntegrationSourceCompiler`'s existing reproducibility rule), so an identical sprint state and token budget always rebuilds an identical digest with no transcript needed.
- [x] P10.13–P10.20 — Add a versioned declarative context-query plan for bounded read-only Git/file/`rg` operations. Validate paths, operation and result limits, source commit, and profile capabilities; return only a selected structured result bundle, never execute model-authored scripts or shell pipelines as a context engine. **Closed 2026-08-17:** `ContextQueryPlan` (`context-query-plan.schema.json`) declares 1-20 bounded operations against one pinned commit: `git_show` (exact file content) and `git_grep` (pattern search), chosen over spawning an external `rg` process specifically to avoid materializing a working tree and adding a new binary dependency — both operations read directly from Git's own content-addressed object store instead. `Forge.Infrastructure.GitContextReader` validates the whole plan (schema shape, operation-id uniqueness, path/pattern safety, and every operation's required capability present in the caller's execution-profile capability allowlist) before executing anything; one invalid or unauthorized operation rejects the entire plan. The reproducible `ContextResultBundle` (`context-result-bundle.schema.json`) records only digests and metadata, never raw content, matching `handoff.schema.json`'s existing digest-only artifact shape — because both operations are pure functions of an immutable commit, replaying the same plan against the same commit always reproduces byte-identical results.

**Gate:** context is bounded, sprint-scoped, reproducible, and based on exact source evidence; query plans cannot mutate the project or widen capabilities, and their result bundles rebuild byte-for-byte from recorded inputs. The MVP owns no full-text, Tree-sitter, LSP, graph, SCIP, or semantic index.

## Stage 11 — `implementation-critical` workflow and parity

- [x] P11.1–P11.12 — Implement intake, planning, threat/rule rubrics, and a task DAG with separate isolated implementation, confirmation, and test-work nodes, followed by deterministic final gates, review, human approval, and finalization for every managed project. Confirmation evaluates the DoD or user expectations; only its valid artifact makes recorded risk-based test selection and authoring eligible. Host state transitions must reject premature test work. Existing checks may support confirmation. A no-new-test result requires evidence that the change adds no behavior or existing checks cover every material risk. Use behavior nodes and rubric data, not a seven-role catalog. **Closed 2026-08-17:** ADR 0013 adds `NodeRole` (a purely descriptive tag on `NodeDefinition`, `TestWork` the one role the scheduler treats specially) and `ImplementationCriticalGraphBuilder`, a pure builder for the one canonical `implementation-critical` graph (`intake -> planning -> implementation -> confirmation -> test_work -> review -> human_approval -> finalization`) that `SprintOrchestrator.CreateSprintAsync` now uses by default for every managed project. `ConfirmationArtifact` (`confirmation-result.schema.json`) is a confirmation node's recorded judgment against its definition of done, with inspection/execution/existing-check evidence matching AGENTS.md's own Quality wording; `SprintScheduler.IsTestWorkEligibleAsync` is the single chokepoint — checked in both `AdvanceGraphAsync`'s promotion loop and `StartAttemptAsync` — that keeps a `TestWork` node from becoming eligible until every confirmation-role dependency's artifacts *at their own latest `RecordedAt`* (all of them, on a tie, so an unspecified enumeration order can never pick a winner) are all `Confirmed`, closing "Host state transitions must reject premature test work" with the newly implemented `workflow_blocked` diagnostic (previously reserved in `docs/contracts/v1/README.md` but unimplemented) — verified end-to-end even through an operator's `resume_sprint`/`run_sprint` call, since eligibility is re-checked from the artifact itself, not the sprint's blocked state. `RecordConfirmationAsync` rejects an unknown node id or a node not tagged `Confirmation` (`node_not_found`/`node_kind_mismatch`) rather than recording an artifact that gates nothing real. A `NotConfirmed` verdict blocks a running sprint with a new `confirmation` `BlockedReasonArgument`, mirroring how a late open finding already blocks one; that append is retried on a version conflict rather than fired once, since it is the sprint's only operator-visible signal. `BuiltInRubric` is a small, fixed threat/rule catalog (`Forge.Domain`) — plain data, not a role catalog, matching the item's own "behavior nodes and rubric data, not a seven-role catalog" wording; no assessment/evaluation type accompanies it, since nothing produces, consumes, or persists one yet. What a test-work node does with its eligibility (select tests, or record a justified no-new-test decision) and what actually evaluates the rubric against a real change are both explicitly deferred to this stage's later execution-profile/provider-attempt items (P11.13 onward) — the same "shape now, producer later" gap ADR 0009 left for `Handoff`, confirmed still true here by the same grep: nothing constructs a test-work result yet.
- [x] P11.13–P11.20 — Freeze only planning, implementation, and review execution profiles, including ordered provider candidates and capability allowlists. Every model node starts without a parent transcript and receives only its frozen context manifest and profile capabilities; a node cannot widen them or invoke human-only commands. Internal/external review share one engine and profile with fresh attempts and bounded inputs; finalization is deterministic. **Closed 2026-08-17:** ADR 0014 moves `ExecutionProfile`/`ExecutionPhase`/`ExecutionLineage` into `Forge.Domain` (correcting a Domain→Application layering inversion the shape had carried since Stage 8) and adds `ExecutionProfilePolicy.Freeze`, a pure function `SprintOrchestrator.CreateSprintAsync` now calls once per sprint to freeze exactly three profiles onto `SprintDefinition.ExecutionProfiles`: planning and implementation both use `FrozenProviders[0]`; review prefers the first candidate with a different provider id (best-effort lineage independence, `AchievedIndependence` recorded either way, never a gate) or falls back to the same provider in a single-provider configuration. `ILlmProvider` gains a vendor-owned `DefaultModel` property (`ClaudeLlmProvider` "sonnet", `CodexLlmProvider` "gpt-5" — the exact pairing `ExecutionProfileTests.cs`/`contract-cases.json` already established as this repo's own canonical example values, not new vendor knowledge invented here), keeping every model string inside its own adapter, never in neutral code. `ExecutionProfilePolicy.PhaseFor(NodeRole)` maps the three model-bearing roles to their phase (every other role has none, matching ADR 0006's "finalization is deterministic code, not a model phase" extended to every other non-model role). Neither half of "cannot widen them or invoke human-only commands" has enforcement code: both are honestly vacuous today (no node executor exists anywhere that makes a capability request for anything to check against a frozen allowlist), and two successive attempts at a capability-containment guard function were each removed after review found zero production callers and, in the first, an unreachable branch (a model node's allowlist and a Host-protocol human-only command use disjoint vocabularies with no code connecting them) — real enforcement lands once a node executor exists to make that request in the first place. `CapabilityAllowlist` uses the real `ContextCapabilityIds.GitShow`/`GitGrep` vocabulary, replacing placeholder strings the schema's own tests had used since Stage 8. Explicitly deferred, and named as such rather than silently gapped: true per-node context-manifest scoping (the one sprint-scoped manifest ADR 0012 already freezes stands in for it, since nothing yet needs per-node content difference), the node executor that would actually call `ILlmProvider.RunAsync` (confirmed by repo-wide search: still zero production callers), and a real per-project model-policy configuration surface (every value with no other real source today — effort, sandbox/permission policy, deadlines — is a fixed, `ponytail:`-marked MVP default).
- [x] P11.21–P11.31 — Implement the ASD review engine: separate design/implementation counters, fresh contexts, full-first/incremental-later scope, file/rubric coverage, same-iteration approval, severity floors, repeated normalized-finding detection, and human convergence gates. Select a different provider/model lineage first, then fall back in normal configured priority when none is available; record achieved separation without creating a human gate. **Closed 2026-08-17:** ADR 0015 adds `ReviewConvergencePolicy` (pure: the cumulative low=1/medium=1/high=2/critical=10 severity-floor table, coverage-ledger completeness, and repeated-consecutive-external-finding-set detection by file/location/rule/message-fingerprint) and `SprintScheduler.RecordReviewIterationAsync`, which persists one `ReviewIterationRecord` per review-dimension iteration (derived count, never caller-supplied), routes each `ChangesRequested` finding through the existing `Finding` machinery (`Open` at or above the floor via unchanged `RecordFindingAsync`; `Dismissed` from the start below it — "dropped, not silently lost" reusing the existing status rather than a parallel list), and blocks the sprint (reason `review_convergence`, requiring explicit `resume_sprint`/`run_sprint`) on either the cumulative iteration limit or two consecutive identical external finding sets, newly implementing the `review_iteration_limit`/`review_repeated_findings` diagnostics reserved since an earlier stage. `PinReviewFloorAsync` is the one new mutating capability beyond recording verdicts — the convergence gate's "continue at critical" choice; "accept findings" and "abort" need no new code, reusing `ResolveFindingAsync` and `CancelSprintAsync`. ADR 0008/0014's reviewer-lineage selection (`ExecutionProfilePolicy.SelectReviewProvider`) is reused as-is, not reimplemented. Explicitly deferred, and named as such: the node executor and any real reviewer dispatch (nothing routes a `ChangesRequested` verdict back into a fresh implementation attempt yet — that needs the same executor ADR 0014 already deferred), which dimension triggers when (the built-in graph has one `review` node, not separate design/implementation ones), multi-reviewer same-iteration aggregation (this item models one combined verdict per iteration, since no multi-reviewer configuration exists to aggregate across), and the full-scope-first/incremental-later input-construction choice (a dispatch/prompt concern, not a policy one).
- [x] P11.32–P11.40 — Replace prompt arguments/buffered output with stdin, minimal child environments, bounded concurrent JSON/JSONL streams, safe tails, and typed activity. Require exactly one schema-valid terminal result for the owned attempt; zero exit without it, duplicates, and contradictions fail closed. **Closed 2026-08-18:** ADR 0016 moves the prompt from a trailing `["--", prompt]` CLI argument to `ProcessRequest.StandardInput` for both adapters (also fixing a latent discrepancy: ADR 0002 already documented Codex's `exec --json` as having no positional prompt at all). `ProviderEnvironmentPolicy.BuildMinimalEnvironment` is the one fixed platform/home-temp/locale/proxy/toolchain allowlist plus each adapter's own (currently empty, since both vendors authenticate through their local credential store, not an env var) authentication-variable names; `ProviderExecution.RunAsync` applies it with `ReplaceEnvironment: true` so a child never sees the host process's own environment, a nested-session marker (`CLAUDECODE`, `CI`), or another provider's credentials — exclusion is by omission from the allowlist, not a denylist. A new private `BoundedOutputSink` (`IProcessOutputSink`) parses stdout incrementally as the already-reworked `ProcessRunner` streams it, enforcing a 1 MiB per-line bound, a 20,000-event bound, a 64 MiB aggregate bound, and an 8 KiB redacted safe tail attached to any bound-violation failure; every extracted event `Text` is redacted through `SecretRedactor` before it leaves this helper. Exactly one `Result`-classified event is required: zero produces the new `ProviderFailureKind.MissingTerminalResult`, more than one produces `ProviderFailureKind.DuplicateTerminalResult` regardless of content agreement — `ProviderRunResult` gains a minimal `TerminalResult` (`Summary` only; neither vendor publishes a stable success-field name, so none is invented). `ILlmProvider.RunAsync` gains an optional `onActivity` callback, invoked once per parsed event with the mapped `AttemptActivityKind` (unthrottled — throttling the persisted write is left to whichever future caller records it), threaded through both adapters. Fixed, as part of this same item since the new uniqueness check surfaced it: `CodexLlmProvider.Classify` previously matched any `turn.*` type as terminal, misclassifying the `turn.started` lifecycle marker alongside genuinely terminal `turn.completed`/`turn.failed`. Explicitly deferred, and named as such: a durable schema/codec for `ProviderTerminalResult` — nothing persists a provider run result yet (same "no node executor exists" gap every prior Stage 11 item has noted; `ILlmProvider.RunAsync` still has zero production callers), so a `docs/contracts/v1/schemas/*.schema.json` file would validate against no real consumer; and the pre-existing `ProviderInstallation` probe path's own additive-environment behavior, out of scope since ADR 0006 frames this item as long-running prompt execution, not install/update probing.
- [x] P11.41–P11.47 — Add absolute/idle deadlines, distinct outcomes, and whole-process-tree cleanup verified on Windows, Linux, and macOS before any native containment adapter. **Closed 2026-08-18:** ADR 0017 adds `Forge.Application.AttemptSupervisor` — the two frozen deadlines from `ExecutionProfile.SessionDeadlineSeconds`/`IdleDeadlineSeconds` (ADR 0014, previously frozen but unread by any code), backed by two `System.Threading.Timer`s: an absolute session timer armed once, and a self-rescheduling idle timer that re-derives the true remaining time from an atomic last-activity timestamp on every tick (rather than resetting via `Timer.Change` from the activity callback, which cannot recall an already-dispatched callback and would still leave a race). `OnActivityAsync` (passed as the `onActivity` callback P11.32-P11.40 already added to `ILlmProvider.RunAsync`) just records that timestamp — "any bounded stream activity resets the idle deadline; model wording does not" holds because that callback never inspects an event's kind or text, so every parsed event counts equally. Whichever fires first (session timeout, idle timeout, or the caller's own token) is latched as `AttemptTerminationReason` and cancels a linked token; `SuperviseAsync<T>` translates the resulting `OperationCanceledException` into a classified `AttemptSupervisionResult<T>` only when this supervisor caused it itself, reusing `ProviderExecution`'s own self-cancellation pattern from ADR 0016 — any other exception the work raises propagates unchanged. Two new diagnostic codes, `ProviderDiagnosticCodes.IdleTimeout`/`.SessionTimeout` (`provider_idle_timeout`/`provider_session_timeout`), were already reserved in `docs/contracts/v1/README.md` since Stage 8 and needed no new persistence plumbing: `SprintScheduler.CompleteAttemptAsync`'s existing `diagnostics` parameter is already exactly where a future node executor would record one. Process-tree teardown is verified with a genuine two-generation tree (child + grandchild, each writing its own process id so the test can positively confirm liveness/death via `Process.GetProcessById` rather than inferring it from OS-varying file-lock semantics) across cancellation, and a companion "normal parent exit leaves no orphan" baseline, both cross-platform (`powershell.exe` on Windows, `/bin/sh` on Linux/macOS) — the existing `Process.Kill(entireProcessTree: true)` needed no change, since these tests confirm the BCL guarantee already holds on all three platforms; no ADR 0007 native containment adapter was needed. Explicitly deferred, and named as such: wiring `AttemptSupervisor` into an actual attempt-execution loop and recording a cleanup outcome beyond the diagnostic code itself both need the same node executor every prior Stage 11 item has deferred — `ILlmProvider.RunAsync`/`SprintScheduler.StartAttemptAsync` still have zero production callers, reconfirmed by repo-wide search.
- [x] P11.48–P11.55 — Implement durable rate-limit deferral and human-only attempt supersession with confirmation, version, idempotency, bounded instruction, cancellation, worktree discard, linkage, and clean replacement. **Closed 2026-08-18:** ADR 0018 gives `RoutingLedger` (built in Stage 8, previously zero production callers) its first caller: `StartAttemptAsync` now calls `DecideAsync` before arming any model-bearing node (Planning/Implementation/Review, via `ExecutionProfilePolicy.PhaseFor`), failing closed with the matching `DiagnosticCodes.Routing*` code on a non-`Routed` outcome. New `DeferAttemptAsync` is the rate-limit-abandonment path: finds the attempt's own `Routed` decision, delegates to the existing `CompleteAttemptAsync(succeeded: false, ...)` with a new `ProviderDiagnosticCodes.RateLimited` diagnostic, and only once that reports success records a deferral via `RoutingLedger.RecordDeferralAsync` at `clock.UtcNow + DefaultRateLimitBackoff` (one minute — no structured provider metadata parser was added, since no vendor reset-time field name is verified in this codebase, matching this stage's established discipline). New `SupersedeAttemptAsync` implements the human-only command literally: confirmation, expected version, idempotency key, and a 4000-character-bounded instruction are all checked before any mutation; on success it cancels the target attempt (`AttemptChanged` → `cancelled`), appends a new non-transition `AttemptSuperseded` event carrying the instruction (new `ISprintStore.AppendAttemptSupersededAsync`, validated in `WorkflowFold.IsTransitionRecord`, idempotent by attempt id so a replay with different instruction text never appends a second, contradictory record), creates a fresh attempt carrying two new `AttemptSnapshot` linkage fields (`SupersedesAttemptId`, and the same `BaseCommit` when the original recorded one), and re-arms the node from `Running` back to `Ready` through the two separately-gated steps the state machine actually has (`Running → Failed → Ready`; there is no direct `Running → Ready` edge) — each step checked against the node's *current* state so a crash-resumed retry finishes only whichever step is outstanding. A genuine idempotent replay is recognized by outcome (target attempt already `Cancelled`), checked *before* the version/terminal checks, so it never re-validates a version the attempt has since legitimately advanced past — the same resumability pattern `ResolveHumanGateAsync` already established. Which specific attempt a `ready` node's next `StartAttemptAsync` call should pick up — an ordinary fresh one, or a pending human-initiated replacement — is answered by two durable facts tracked directly rather than reconstructed from a counter: a new `NodeSnapshot.CurrentAttemptId` records exactly which attempt a node was started with, and `StartAttemptAsync` finds any pending replacement by a direct query (`created`, linked to this node) instead of recomputing its id. Six independent review rounds (three full-scope, the last three critical-findings-only per this repository's review-gate rules) found and fixed eleven further defects before merge, each with its own regression test (a diagnostics-registry documentation gap does not count toward this total — it changed no behavior); ADR 0018 records the complete history, including four successive bugs in an earlier design that tried to reconstruct the pending replacement's identity from `NodeSnapshot.AttemptCount` and a deterministic-id guess (fixed by replacing that mechanism outright with the direct-tracking design above) plus one further defect the redesign itself introduced: `StartAttemptAsync`'s own fresh-attempt path lacked the same collision-skip its sibling creation path in `SupersedeAttemptAsync` had just gained, so a number consumed by a collision-skip elsewhere could still collide with a later, unrelated fresh start; fixed by giving both paths the identical collision-skipping walk. Together with the two bugs the initial implementation's own tests already caught (the illegal direct re-arm transition, and the first replay-vs-stale-version ordering issue), every fix is documented in ADR 0018 with the regression test that proves it. Explicitly deferred, and named as such: process-tree cancellation and worktree discard (ADR 0006's "cancels the process tree, discards the owned worktree") — `SprintGitIsolation` explicitly holds no executor, and none exists anywhere in the repo yet to hold the live resources being cancelled/discarded, the same gap every prior Stage 11 item has deferred to the same future executor; human-only *enforcement* (which command surfaces expose `attempt.supersede` and to whom) belongs to P11.56–P11.66's CLI/TUI commands, not the scheduler primitive; and circuit-breaker wiring for ordinary (non-deferred, non-rate-limit) failures, out of scope for durable rate-limit deferral and budget sanity, both of which this item now fully covers without it.
- [ ] P11.56–P11.66 — Complete CLI/TUI and Desktop projections, commands, attention navigation, human gates, recovery, English/Russian localization, configuration editors, accessibility, and parity tests. Includes the snapshot fields re-scoped from P8.25–P8.33 — integration status, phase profile, and active deadline — which only this stage's executor first produces. **Progress 2026-08-18:** ADR 0019 lands the first of several planned slices (matching how P8.25–P8.33 itself closed across three passes): `forge gate approve|reject` and `forge attempt supersede <attempt-id> --instruction-file <path|->` now call `SprintScheduler.ResolveHumanGateAsync`/`.SupersedeAttemptAsync` (both zero-caller since their introducing stages) end to end through the Host — new `IForgeMutations.ResolveGateAsync`/`.SupersedeAttemptAsync` derive the target's expected version and idempotency key server-side from a fresh `ISprintStore` read rather than exposing a raw entity version on the snapshot; new `resolve_gate`/`supersede_attempt` `ControlProtocol` kinds and Host dispatch handlers route the mutation through the project's Host per ADR 0005, serializing `NodeActionResult`/`CompleteAttemptResult` directly (the same precedent `ProjectSnapshot` already set). Neither command accepts a config-driven confirmation bypass (`interaction.confirm_destructive` cannot silently disable `--yes`, unlike `InstallIntegrationAsync`/`RemoveIntegrationAsync`). ADR 0019 is explicit that this slice ships **no actual technical control** for ADR 0005's "human-only" requirement — no caller-identity or authorization mechanism exists anywhere in this codebase, and a project's own `.forge/rules` can instruct an agent to invoke either command by name regardless of what the generated integration text otherwise contains; mandatory confirmation is a policy-level speed bump against accidental invocation, not an enforced boundary, and a real technical control is named as deferred future work rather than silently assumed solved. An independent review round found and fixed two critical defects in the already-shipped `SprintScheduler.ResolveHumanGateAsync`/`.SupersedeAttemptAsync` (ADR 0018) their first real callers exposed: both methods recognized a resumed call only by recomputing an exact version/idempotency-key hash, which a caller with no memory of the original call (a stateless CLI invocation re-run after a crash) can never reproduce once the target has moved past its initial state — permanently refusing every retry instead of resuming it. Both are now recognized by durable linkage instead (an attempt's `NodeId` for gates; the attempt's own `Cancelled` state for supersession), with the gate fix additionally guarding explicitly against a caller flipping the decision, since the attempt itself does not separately record which way it was decided. Review also found and fixed unbounded reading of the "bounded" instruction artifact (now capped at read time, not just after buffering it whole), three diagnostic-code mismatches, a literal `"human_approval"` string duplicating `ImplementationCriticalGraphBuilder.HumanApprovalNodeId`, and two test gaps (the no-bypass confirmation requirement and an unreadable-instruction-file case were previously unverified). Two new diagnostic codes (`supersession_instruction_unreadable`, `supersession_instruction_required`) distinguish an unreadable source from an over-length or empty one. **A second review round found the gate linkage fix above was itself a regression**: matching by bare `NodeId` also matches an ordinary `Work` node's own in-progress attempt (it carries the same `NodeIdArgument`), letting `forge gate approve` hijack and fraudulently "succeed" a live, unrelated node; and a rejected gate retried back to `awaiting_human` for a second decision would have its linkage resolve to the first, terminal attempt from the earlier round instead of a fresh one. Fixed by checking the target is actually `NodeKind.HumanGate` before any attempt lookup (`DiagnosticCodes.NodeKindMismatch` otherwise, mirroring `StartAttemptAsync`'s own `NodeKind.Work` gate), and by scoping linkage to only apply once the node has moved past `awaiting_human` — a node still `awaiting_human` is handled entirely by the deterministic-hash fresh path, and once past it, the most-recently-updated linked attempt (`OrderByDescending(a => a.UpdatedAt)`) is always the current round's. Also fixed: `Forge.Cli.ExitCodes.For` had no case for any of the three `supersession_instruction_*` diagnostic codes despite them being documented as exit 2 — all fell through to `Internal` (13); this item's own `forge attempt supersede` is what first makes them reachable, so the gap is closed here rather than left latent. **A third review round found two more critical defects.** First, a `HumanGate` node's `running` state is ambiguous between `ResolveHumanGateAsync`'s own approve walk and `AdvanceGraphAsync`'s unrelated two-append gate promotion caught mid-sequence (which creates no attempt at all) — in the latter case, linkage still found an earlier round's stale, terminal attempt, and `DriveAttemptAsync` reported success for what was actually its own documented off-path no-op, silently driving the node to `succeeded` with no attempt or result ever recording the decision. Fixed generally: after `DriveAttemptAsync` reports success, the attempt's actual resulting state must equal the target path's own terminal state or the call is refused (`DiagnosticCodes.AttemptTerminal`) instead of proceeding — closing every off-path no-op regardless of cause without weakening genuine resumability. Second, `SupersedeAttemptAsync` had no node-kind guard at all (unlike `StartAttemptAsync` and the now-fixed `ResolveHumanGateAsync`): superseding a human gate's attempt minted a `created` replacement nothing could ever start and re-armed the node through a `Running -> Failed -> Ready` sequence shaped for a `Work` node's state machine. Fixed the same way: refused with `DiagnosticCodes.NodeKindMismatch` for any target node that is not `NodeKind.Work`. Review also added the one missing CLI exit-code test (`supersession_instruction_too_long`, the pre-existing code the other two new ones were modeled on) to match the two already covering the new codes. Explicitly deferred to later slices of this same item, and named as such: sprint-lifecycle commands (`create`/`run`/`resume`/`cancel`; `rebase` has no sprint-level backend yet), the two named snapshot-field additions (integration status, phase profile), Desktop parity (`SurfaceParityTests` requires a Desktop control for every `CapabilityIds.Implemented` entry, so neither `workflow.review` nor `attempt.supersede` is added to that list yet), ICU localization, accessibility work, and a real technical human-only control. The node executor itself remains out of scope, same as every prior Stage 11 item — superseding an attempt cancels whatever a future executor would have run and creates a linked replacement for that executor to eventually pick up. **Progress 2026-08-18 (second slice, ADR 0020):** `forge sprint create|run|resume|cancel` now call `SprintOrchestrator`'s four verbs (all zero-caller since Stage 6) end to end through the Host, following the exact pattern ADR 0019 established — new `IForgeMutations.CreateSprintAsync`/`RunSprintAsync`/`ResumeSprintAsync`/`CancelSprintAsync`, matching `ControlProtocol` kinds/dispatch/`RemoteForgeMutations` wiring. `run`/`resume`/`cancel` derive their expected version and idempotency key server-side from a fresh `SprintOrchestrator.GetSprintAsync` read; `create` cannot follow that convention (a sprint's creation key can't be derived from "the project's current version", which never changes on creation) and instead mints a fresh `Guid.NewGuid()` per call — an accepted trade-off documented in ADR 0020 rather than silently glossed over: a crash-then-retry of `forge sprint create` is not guaranteed idempotent and could orphan an invisible, never-listed sprint directory (never a duplicate *visible* sprint or corrupted state), with a future `--idempotency-key` option named as the likely fix. `create`/`run`/`resume` take no confirmation (additive, not destructive); `cancel` follows the ordinary bypassable-confirmation pattern (`interaction.confirm_destructive`), not the gate/supersede pair's mandatory one, since `sprint.manage` carries an ordinary `workflow_mutate` permission in `capabilities.json`, not a human-only one. `run`/`resume` share one command builder but not one success message — `run`'s message reports whatever state the sprint actually settled at (its own `AdvanceGraphAsync` side effect can promote further than the one legal hop `run` itself performs, e.g. straight into `awaiting_human` when the graph opens with a gate), while `resume` always targets exactly one state and uses fixed text; an early version wrongly shared `run`'s dynamic message for both, caught by `resume`'s own acceptance test before landing. The same builder also defends against a `null` `Sprint` on an otherwise-successful result (a `NullReferenceException` the routing acceptance test's own fake caught before landing), matching the same defensive shape `create`'s output already needed for a possibly-`null` `SprintId`. Explicitly deferred to later slices, and named as such: `forge sprint rebase` (no sprint-level backend exists yet), Desktop parity (`sprint.manage` is not added to `CapabilityIds.Implemented` in this slice), the crash-safe create-retry option, ICU localization, accessibility work, and a real technical human-only control (unrelated to this slice, still open from ADR 0019). **An independent review round found and fixed two more critical defects in already-shipped, zero-caller `SprintOrchestrator` code this slice's first real callers exposed** (the same pattern every prior Stage 11 review round has hit): `RunSprintAsync` returned its pre-`AdvanceGraphAsync` snapshot, so `forge sprint run` reported `running` while the durable state had already moved past it into `awaiting_human` when the graph opens with a gate — fixed by returning `AdvanceGraphAsync`'s own result instead. Far more seriously, `ResumeTarget` mapped `awaiting_human` to `running` (not just `blocked`/`failed` to `ready`), so `forge sprint resume` on a sprint parked at a pending human gate silently bypassed it — exit 0, sprint flipped to `running`, the gate node left `awaiting_human` forever, and `StartAttemptAsync`'s own `Sprint.State == Running` gate meant a `Work` node's attempt became startable while the human decision was still pending, reachable without ever touching `forge gate approve|reject`. Fixed by removing that mapping entirely, so `resume` against an `awaiting_human` sprint is now refused with `SprintTransitionInvalid`. Review also found `forge sprint cancel` does not cascade to live nodes/attempts (nothing marks them terminal, and `AdvanceGraphAsync`'s promotion loop is not gated on sprint state, so a later unrelated call can still promote nodes inside an already-cancelled sprint) — explicitly added to ADR 0020's deferred list rather than fixed in this slice, since a correct fix needs its own design. Both fixed bugs carry regression tests, and a real Host round-trip test (`ControlPlaneTests.SprintLifecycleCommandsRoundTripThroughTheHost`) now exercises all four new wire kinds, matching ADR 0019's own precedent — the original PR shipped with only a fake-backed routing test that could not have caught a wire-level mismatch. **A third (final full-scope) review round corrected the ADR's understated create-idempotency risk claim**, fixed `RunSprintAsync`'s `InvalidOperationException` catch to be genuinely narrow (it briefly swallowed every such exception, not just the missing-definition case it exists for, reporting silent success even for durable-state invariant violations) instead of catch-all, and fixed a dangling-sentence-fragment CLI message. It also found — and this time, rather than fix, explicitly deferred after a fix attempt made things worse — that `forge sprint create` has no guard against a second non-terminal sprint (silently blanking `forge status`/`forge next`'s active-sprint target on the ordinary, no-crash path): a guard was implemented, then reverted, because multiple non-terminal sprints coexisting in one project is a deliberately tested, intentional scenario elsewhere in this codebase, and the naive guard's own implementation (eagerly loading every existing sprint's state) reintroduced exactly the cross-sprint-isolation failures several existing tests exist to prevent. Also newly deferred: `forge sprint resume` on a gate-rejected sprint un-blocks the *sprint* but not the rejected gate *node* (nothing re-arms it, since no CLI command exposes `SprintScheduler.RetryNodeAsync`), leaving the sprint durably stuck at `running` with no further CLI-reachable progress — the slice's own flagship `resume` acceptance test now asserts this directly instead of leaving it implied. Both deferrals are named in ADR 0020's "Deliberately deferred" list, matching the `cancel`-cascade precedent from the prior round. **Progress 2026-08-18 (third slice, ADR 0021):** the Desktop app can now approve or reject a human gate, the first Desktop-side slice of this item — `workflow.review` was chosen over `sprint.manage`/`attempt.supersede` as the smallest well-scoped target (a single approve/reject action against an existing backend, needing the fewest new controls). Landed directly on the existing flat `MainPage` rather than behind a new navigation shell: no navigation framework exists anywhere in `Forge.Desktop` today (not even unused scaffolding), every capability implemented so far already lives on that one page, and `SurfaceParityTests` has no concept of "which page" a control lives on — building a real navigation shell is a substantial, separate design decision deserving its own slice, not something to bundle into two buttons for one capability. The existing `SprintIdEntry` is reused as the gate's sprint context rather than duplicated; a new `GateNodeIdEntry` defaults to the canonical `human_approval` node when empty, matching the CLI's own `--node` default. Confirmation is the `DisplayAlertAsync` dialog's real yes/no answer passed straight through to `IForgeMutations.ResolveGateAsync`, mirroring the existing `RecoverAsync` control's shape — never bypassed by construction, matching ADR 0019's no-config-bypass rule for this capability. `CapabilityIds.WorkflowReview` is now in `CapabilityIds.Implemented`. Explicitly deferred, matching every prior slice's own sizing rationale: a real navigation/attention-navigation shell (still the largest remaining piece of this item), Desktop controls for `sprint.manage`/`attempt.supersede`, and every other Desktop-absent capability, most of which have no backend caller at all yet. **Two review rounds found and fixed real gaps before merge.** Round 1: the "confirmation never bypassed" property (and the reject path entirely) had zero test coverage — a hardcoded `true, true`/`approved: true` would have passed the whole suite; a blank sprint id (the page's own default state) always failed with `sprint_not_found` instead of targeting the active sprint like the rest of the page already does; declining the confirmation dialog still round-tripped a real request to the Host; the dialog never named its target; and a stale gate-result message could survive into an unrelated later refresh. Round 2 found the round-1 active-sprint fix still collapsed two materially different `null` cases from `StatusAdvisor.DetermineActiveSprint` (genuinely no sprint, vs. more than one non-terminal sprint with Forge deliberately declining to choose) into the same `sprint_not_found` message, telling a user with several running sprints that nothing was found while those sprints sat in the tree directly above the button; fixed with a dedicated `GateSprintAmbiguous` message resolved entirely client-side, plus a test proving which specific sprint id a blank entry actually resolves to (the round-1 fix's own claim had gone unverified the same way the round-1 confirmation gap had). **Progress 2026-08-18 (fifth slice, ADR 0022):** the Desktop app can now supersede a non-terminal attempt (`attempt.supersede`), reusing `workflow.review`'s own design directly — the same confirm-then-call/never-bypassed shape, and `MainPageViewModel.ResolveSprintIdAsync`/`SprintTarget` itself (not a second copy) for the identical blank-means-active-sprint/ambiguity targeting logic two prior review rounds hardened. `sprint.manage` was considered first but rejected: its `capabilities.json` entry documents `forge sprint <create|run|resume|cancel|rebase>`, and `rebase` has no backend yet, so marking it `Implemented` would require a CLI subcommand this slice cannot build — deferred to a future slice that designs `rebase` first. This slice's own capability exposed a latent bug in `SurfaceParityTests`'s CLI-parity check: its `Alternatives` helper misparsed `attempt.supersede`'s `--instruction-file <path|->` (an option's value grammar) as alternative subcommand names, since no capability before this one had a `<a|b>`-shaped token following a `--flag`; fixed by stopping the scan at the first option token. **An independent review round found and fixed four more real gaps before merge**: the instruction's empty-vs-over-length check order diverged from the CLI (Desktop checked emptiness first, the CLI checks the length bound first, so a whitespace-only over-length instruction reported the wrong diagnostic on each surface) — fixed by matching the CLI's order; a misleadingly-named `trimmedInstruction` variable never actually trimmed anything (correct behavior, since the CLI forwards verbatim, but the name invited a future "fix" that would have silently diverged the two surfaces' recorded text) — renamed and pinned with a whitespace-preserving test; a blank attempt id let the confirmation dialog show with no target named at all, asking the user to confirm an irreversible action against nothing before failing afterward with an unhelpful `workflow_event_conflict` — fixed by refusing before the dialog shows, plus a placeholder in the prompt itself as defense in depth; and the length bound's test coverage only exercised the rejecting side (`Max + 1`), so an off-by-one rejecting at exactly `Max` would have shipped silently — fixed with an accepting test at exactly `Max`. The same round also strengthened `SurfaceParityTests.CliExposesEveryDocumentedCapabilityCommand` itself, which `attempt.supersede` was the first capability to expose a gap in: it never verified a literal subcommand token (`supersede`) actually exists as a subcommand, only options and `<a|b>`-alternatives — fixed by walking and asserting each literal token at its documented depth. **A second review round found three further gaps**: the blank-sprint-id ambiguity branch still returned the gate surface's own "resolve its gate" wording for a capability with no gate at all — fixed with a dedicated `AttemptSupersedeSprintAmbiguous` message and a regression test for the one branch that previously had none; round 1's own literal-token-walk fix resolved the correct subcommand but still checked options/alternatives against the top-level command, undoing its own tightening — fixed by checking against the resolved subcommand instead; and ADR 0022's own consequences list omitted two of the seven message keys it introduced by name — completed. **A third (final full-scope) review round found two more gaps**: the blank-attempt-id guard had shipped with no regression test at all — deleting it left the full suite green — fixed with a code-behind text assertion pinning both its presence and that it runs before the confirmation dialog; and a blank replacement instruction was refused only after the user confirmed the irreversible action, asymmetric with the attempt-id guard's own rationale — fixed with a matching pre-dialog emptiness check (the length bound stays server-validated) and its own regression test. **A fourth (critical-only) review round found no critical defects** and posted no findings with no threads opened, passing the review gate; it noted one accepted, documented trade-off (a whitespace-only-and-over-length instruction now reports the pre-dialog "required" message rather than the more specific "too long" one, since the length bound intentionally stays server-side only). **Progress 2026-08-18 (sixth slice, ADR 0023):** `forge gate approve|reject` and `forge attempt supersede` now refuse to run unless invoked from an interactive terminal (`!Console.IsOutputRedirected`), the first real technical control behind ADR 0005/0019's "human-only" requirement — every prior slice since ADR 0019 named this as deferred without anyone picking it up. Implements the long-reserved `permission_denied`/exit 8 diagnostic (zero production references before this slice, the same "reserved but unimplemented" gap `workflow_blocked` was before P11.1-12). Honestly scoped as raising the bar, not closing the gap: an agent that explicitly allocates a pseudo-terminal still passes: the ADR is explicit this is not unforgeable caller identity, just a real, non-trivial obstacle against the specific vector every prior ADR actually flagged (an agent shelling out to the command the same way it runs any other). The check runs first and unconditionally, reachable only after System.CommandLine's own argument parsing and before this action's own validation or `--yes` is even inspected — proven for `attempt supersede` by a `ThrowingTextReader` test double that fails the test if the instruction source is ever read, not merely if the mutation is never called. `CreateRootCommand` gained an injectable `isInteractive` parameter matching the existing `input`/`resolveMutations` pattern; the 12 pre-existing gate/attempt CLI tests were updated to pass `isInteractive: () => true` explicitly after discovering the check's underlying `Console` property reads `true` (redirected) under this repository's own `dotnet test` runs, which would otherwise have made them fail non-deterministically on the ambient default. Desktop needed no change: `DisplayAlertAsync`'s dialog already requires a human GUI session by construction. **An independent review round found and fixed a real functional regression plus two smaller gaps.** The initial version checked *input* redirection (`Console.IsInputRedirected`), which broke `forge attempt supersede`'s own documented `--instruction-file -` shape outright: piping an instruction always redirects stdin regardless of who is doing the piping, so every stdin-sourced supersession became permanently unreachable — fixed by switching the signal to *output* redirection (`Console.IsOutputRedirected`), which a human piping instruction text in still satisfies (their terminal remains attached to stdout) the same way common Unix tooling checks `isatty(stdout)` rather than stdin for this exact question. Review also found the ADR's original deferred-work list named only pty allocation as a residual bypass, missing a second, more direct one: `ControlPlaneHostedService`'s Host dispatch handlers have no equivalent check, so a same-user caller speaking the Host protocol directly via `Forge.Host.Client` bypasses this control entirely without invoking the CLI at all — not fixed (a console check is meaningless at a background service with no terminal of its own), but now named explicitly. A third gap — the production default itself had zero test coverage, since every existing test overrode the parameter — was closed with a new test that omits `isInteractive` entirely. Explicitly deferred, matching every prior slice's own sizing rationale: a real unforgeable caller-identity mechanism (now documented as covering both the pty and Host-protocol bypasses), applying the same check to any future human-only command, and every other Stage 11 gap ADR 0022 already deferred (navigation shell, `sprint.manage` Desktop controls, `forge sprint rebase`, ICU localization, accessibility, executor-blocked snapshot fields). **A second review round found six further gaps.** Most notably: round 1's own input→output signal fix traded one false refusal for a narrower, previously-unstated one — a human who deliberately redirects their own output (e.g. `forge gate approve ... | tee log.txt`) is now refused exactly like a non-interactive agent, since no portable signal distinguishes the two; documented honestly as a known, accepted trade-off (a future opt-in `--force-interactive` escape hatch is the likely real fix) rather than left unstated. The round also found two stale doc/comment fragments left over from the original input-based version (`CreateGateCommand`'s own doc comment, and `DiagnosticCodes.PermissionDenied`'s XML doc, both still said "input" after the code itself had moved to output), a false "at the moment the command tree is built" claim in the ADR that round 1 had already corrected in the code comment but not in the ADR text, a comment citing the old property's semantics as authority for the new one, and — most substantively — the round-1 default-coverage test itself hard-coded an outcome that depends on how the test binary is launched (xunit v3 builds a runnable executable; run from a real interactive terminal, the hard-coded assertion would be wrong, not the code) — fixed by computing the test's own expectation from the same real `Console.IsOutputRedirected` property the production default consults, so it is correct under either launch environment. **A third (final full-scope) review round found five further accuracy fixes and no functional defect**: a full re-trace confirmed no gap in which paths reach the mutation and no third redirection-signal problem beyond the two already documented. Fixes: a stale "never cached" phrasing round 2 had already removed from the code comment but left in the ADR; an XML doc wrongly claiming `permission_denied` was reserved "since Stage 8" (`git log -S` confirms Stage 0, matching what the ADR's own Context section already said correctly); a broken sentence; and, most substantively, the round-2 default-coverage test's own doc comment overclaimed symmetric mutation coverage — under `dotnet test`'s always-output-redirected environment, only a default hard-coded to `() => true` is actually caught, while `() => false` is indistinguishable from correct behavior there, narrowed to state this honestly rather than pretend the gap is closed. The `CHANGELOG.md` entry (used verbatim as the GitHub Release description) also omitted the `| tee` false-refusal trade-off both the ADR and PR body already named; added. **Progress 2026-08-18 (seventh slice, ADR 0025):** the Desktop app can now poll for recent workflow events (`control.events`), the third Desktop parity slice — a read-only query rather than a human-only mutation, chosen because it needed no confirmation dialog to harden (the single biggest source of review churn in the two prior slices) and its backend (`ForgeApplication.ReadControlEventsAsync`/`ControlEventsReader`) and CLI (`forge events`) were already fully built with zero Desktop counterpart, unlike every other Desktop-absent capability, which has no backend at all yet. `CliApplication.WriteEvents`'s formatting was extracted into a new `SurfaceFormatting.EventLines`, shared with the new `MainPageViewModel.PollEventsAsync` the same way `SprintTreeLines`/`SprintDetailLines` are already shared, so the two surfaces cannot silently drift. The page instance holds its own stored cursor (mirroring the CLI's own local `cursor` variable inside its `--follow` loop — a manual re-click is the direct Desktop equivalent of one iteration of that loop, not a gap to close), reset whenever the requested project root changes. `EventsLabel` is *not* cleared by `RefreshAsync`'s existing unconditional "a prior action's outcome must never survive an unrelated trigger" rule (`GateResultLabel`/`AttemptSupersedeResultLabel`'s own rule): unlike those two, which carry a one-shot mutation's outcome with no companion state, `EventsLabel` is a live view of the stored cursor, so it is gated on the identical project-root-switch condition instead. A rejected cursor needs no dedicated recovery path, unlike ADR 0024's notification cursor: `ControlEventsReader` already returns a fresh, immediately-usable anchor for it, and redisplaying an already-seen event has no side effect to protect against in a read-only list. `sprint.manage` remains correctly blocked on `forge sprint rebase`, unchanged from ADR 0022. **An independent review round found and fixed two gaps**: the first version cleared `EventsLabel` unconditionally after all (copying the other two labels' rule without checking it applied), discarding a still-valid poll's rendered page on the very next `Refresh` click — fixed by gating the clear on the project-root-switch condition described above; and the CLI/Desktop rendering parity `SurfaceFormatting.EventLines`'s extraction claims had no test proving it — fixed with a dedicated no-drift test matching the sprint-tree/detail parity test's own shape. **A second review round found three further gaps**: this progress note itself still described the pre-fix unconditional-clear behavior, contradicting the ADR the first round had already corrected — fixed to match; `MainPage.xaml.cs`'s `PollEventsAsync` read `ProjectRoot` twice across an `await`, so editing the project-root entry mid-poll could desync the page's own reset-tracking field from what was actually polled — fixed by capturing the root into a local once, before the request; and the round-1 static test proved only text ORDER (guard before clear), which a mutation moving the clear back outside the guard's own block would not have failed — fixed to check CONTAINMENT (the clear is textually inside the guard's `{ }` block) instead. **A third (final full-scope) review round** found the containment check still admitted a one-line mutation restoring round 1's exact defect (deleting the tracking-field assignment inside the guard block leaves the field permanently `null`, verified empirically to leave the suite green) — fixed by also asserting that assignment is present; found this PR's own `RefreshAsync` had introduced the identical two-reads-across-an-`await` shape round 2 had just fixed in `PollEventsAsync`, one method away and not pre-existing — fixed the same way, by capturing the project root into a local once; and removed a dangling forward-reference in the ADR's own Round 2 section. Explicitly deferred, matching every prior slice's own sizing rationale: a real navigation/attention-navigation shell (still the largest remaining piece), `sprint.manage` Desktop controls, every other backend-less Desktop-absent capability, ICU localization, accessibility work, the executor-blocked snapshot fields, and a real unforgeable caller-identity mechanism. **Progress 2026-08-18 (eighth slice, ADR 0026):** the Desktop app can now preview, install, and remove the generated provider agent integration (`integration.skill`), the fourth Desktop parity slice — `generate` is a query (no confirmation, no Host round-trip), `install`/`remove` are ordinarily-bypassable mutations (`integration_write_confirm`, not human-only), copying `RecoverAsync`'s existing bypassable-confirm shape directly rather than `ResolveGateAsync`/`SupersedeAttemptAsync`'s never-bypassed one. Applying `control.events`'s own four-round review history proactively rather than reactively: `CliApplication`'s `generate`/`install`/`remove` bodies were extracted into shared `SurfaceFormatting.IntegrationInspectionLines`/`IntegrationWriteLines` in the SAME commit as a `DesktopAndCliRenderTheSameIntegrationPreviewForOneSnapshot` parity test (not a later review-round fix), and every new async method captures `ProjectRoot` at most once before its own request from the first commit, closing the TOCTOU shape two prior review rounds each found once. `IntegrationLabel`/`IntegrationWriteResultLabel` follow `GateResultLabel`'s unconditional-clear rule (no companion state), not `EventsLabel`'s gated one. `sprint.manage` remains correctly blocked on `forge sprint rebase`, unchanged from ADR 0022. **An independent review round found and fixed four issues**: the install/remove confirmation dialogs still repeated the action name as their own message instead of naming a target, the exact defect two existing static tests already forbid for `workflow.review`/`attempt.supersede` — fixed by reusing `MainPageViewModel.InitializePrompt`'s existing shape; the new parity test's fixture was degenerate (`TestEnvironment`'s only default provider has no matching generator anywhere in this neutral test composition, so both surfaces rendered only the constant "no artifacts" string, exercising neither `IntegrationInspectionRow` nor the document-error rows) — fixed by giving `TestEnvironment` a new optional `generators` parameter and a minimal fake generator; `IntegrationWriteLines`, the mutating half actually reachable from a destructive action, had no parity test at all — fixed with a second test using two separate projects rather than a same-target second install (which would render `unchanged`, not `written`, for a reason unrelated to what the test exists to prove); and the two now-private row-formatting helpers had stale doc comments claiming to be "shared with Desktop" directly rather than through the actual shared, tested surface. **A second review round found round 1's own fixture fix was only half-closed**: round 1 itself named two drift-capable parts of the shared projection (the artifact row and the document-error row), but the new fake generator only exercised the first — both parity tests used freshly initialized projects with no `.forge/rules` parse failures, so the document-error rows this PR newly exposed to Desktop were never rendered by any test on either surface, and a Desktop-side regression there would have left the full suite green. Fixed by writing a malformed rule document into the preview parity test's project before generating, matching `IntegrationGenerationTests`'s own fixture shape. **A third (final full-scope) review round found round 2's own fix was itself asymmetric**: it added document-error coverage only to the preview parity test, but `IntegrationInstallationService.InstallAsync`/`.RemoveAsync` each run their own `InspectAsync` internally and propagate its `DocumentErrors` verbatim, so the write path — the one actually reachable from a destructive action — rendered the identical error row and was still untested there; deleting the error-rendering call from `IntegrationWriteLines` would have left every test green. Fixed by writing the same malformed rule document into both projects in the write parity test. Explicitly deferred, matching every prior slice's own sizing rationale: a real navigation/attention-navigation shell (still the largest remaining piece), `sprint.manage` Desktop controls, every other backend-less Desktop-absent capability, ICU localization, accessibility work, the executor-blocked snapshot fields, and a real unforgeable caller-identity mechanism. **Progress 2026-08-19 (ninth slice, ADR 0027):** the Desktop app can now create, run, resume, and cancel a sprint (`sprint.manage`), the fifth Desktop parity slice — before starting, `rebase` (the fifth `sprint.manage` verb every prior slice's own deferred list named as the blocker) was investigated directly rather than accepted at face value again: it is not a design gap, it is a command with no reachable trigger, since `SprintGitIsolation.RebaseAttemptAsync` (the git-level primitive it would wrap) only recovers from a concurrent-attempt-integration race that cannot occur without a node executor, and no executor exists yet — the same "zero production callers" fact every prior Stage 11 ADR has independently reconfirmed. `capabilities.json`'s `sprint.manage` entry is split: narrowed to the four real verbs, with a new separate `sprint.rebase` capability documenting the blocked verb and explicitly left unimplemented (`contract_version` 1.1.0 → 1.2.0). `create`/`run`/`resume` are not confirmable (additive, matching `ForgeApplication`'s own existing contract); `cancel` reuses `RecoverAsync`/`InstallIntegrationAsync`'s ordinarily-bypassable shape, not the human-only gate/supersede pair's, and names its target via a new `SprintCancelPrompt` from the first commit (ADR 0026's round-1 lesson applied proactively). `run`/`resume`/`cancel` reuse `ResolveSprintIdAsync`/`SprintTarget` directly, with their own dedicated `SprintManageSprintAmbiguous` message key (ADR 0022 round 2's lesson, also applied proactively, with a test proving the message is not either sibling capability's own ambiguity text; **an independent review round later found and fixed five issues here**: the `sprint.rebase` capability note and this progress note's own investigation both cited a nonexistent `SprintScheduler.IntegrateAsync` member -- the real one is `SprintGitIsolation.IntegrateAsync`, the investigation's conclusion itself independently re-verified and unaffected; the `create` parity test's format check never actually compared the CLI and Desktop outputs against each other and skipped one unasserted separator character; `TransitionSprintAsync`'s own copy of the ambiguity branch (shared by `run`/`resume`) had no test at all, the exact ADR 0022 round-2 defect shape pre-empted for only one of two call sites, fixed with a new regression test verified by mutation testing; `CHANGELOG.md` had no entry for the capability-contract split despite being published verbatim as the Release description; and the ADR's own consequences list said "five new message keys" above a list of six). `CliApplication`'s `create`/`run`/`resume` bodies are extracted into shared `SurfaceFormatting.SprintCreatedMessage`/`SprintTransitionMessage`, landed in the same commit as CLI/Desktop parity tests for all four verbs (ADR 0025/0026's lesson applied proactively): `create`'s own test cannot use literal equality (each call mints a fresh id), so it compares message format instead; `resume`'s test uses a genuinely blocked sprint (a rejected human gate), not a fresh one, since a fresh sprint's `resume` call fails and would produce the "empty state on both sides" antipattern ADR 0025 round 1 already rejected once. Explicitly deferred: `forge sprint rebase` and Desktop `sprint.rebase` controls (structurally blocked on the node executor, not a scoping choice), a real navigation/attention-navigation shell, every other backend-less Desktop-absent capability, ICU localization, accessibility work, the two executor-blocked snapshot fields, and a real unforgeable caller-identity mechanism. **A second review round found three further gaps**: round 1's own fix for `TransitionSprintAsync`'s ambiguity branch covered only its `Ambiguous == true` half, leaving the `false` (unparsable/not-found) half still proven exclusively through `CancelSprintAsync`'s separate copy — fixed with a new `RunSprintAsync`-based regression test, mutation-verified; `SprintCancelPrompt`'s blank-sprint branch had no test at all, unlike both prompts it mirrors — the first fix attempt used `Assert.Contains` against the placeholder text and passed even under the applied mutation, because the Russian `SprintIdLabel` string itself already contains the placeholder's own Russian text as a substring regardless of which branch renders, so it was rewritten to assert the full composed string with `Assert.Equal` instead, which fails correctly under any UI culture; and round 1's own `CHANGELOG.md` fix justified itself with "`contract_version`'s first-ever bump in this file", which is wrong — that file's `contract_version` was already bumped once before, `1.0.0` -> `1.1.0` in commit `a7be09f` (v0.11.0) — the inaccurate appositive was removed, the `Changed` entry itself kept. **A third (final full-scope) review round found one further issue**: the PR body's own Verification section claimed `SprintLifecycleCliTests`'s "existing 215-test suite" — the file actually has 8 tests; corrected. The round independently re-verified every prior round's claims and mutation-tested six regressions, all caught, and named two non-blocking, pre-existing gaps out of scope for this slice (untested mutation-failure fallback messages across several capabilities, and a stale `README.md` Desktop paragraph) without treating either as a blocker. Before scoping a tenth slice, every remaining item in this stage's own deferred lists was investigated directly: the four "no backend" capabilities turned out to be mostly out of this stage's own scope (`observability.inspect`/`diagnostics.bundle`/`quality.evaluate` belong to Stage 12 per this plan's own text; `project.sync_validate` has no design and no code at all); the navigation shell would invalidate `SurfaceParityTests`' own `MainPage`-literal-path architecture and has nothing yet worth navigating to; ICU localization has zero resx placeholders needing it; accessibility is small but nearly unverifiable given this repository's inability to instantiate MAUI controls headlessly; and a real caller-identity mechanism needs its own design ADR before any slice. The node executor itself, though larger, was the only candidate with real, ready-to-wire seams and no smaller alternative. **Progress 2026-08-19 (tenth slice, ADR 0028):** the node executor's first slice ships — a new `IntakeExecutionHostedService` (`Forge.Host.Runtime`) drives every running sprint's `intake` node (`NodeRole.Intake`, the one Work role `ExecutionProfilePolicy.PhaseFor` returns no execution profile for, so it needs no provider, prompt, deadline, routing decision, or worktree) through a real attempt: parses the project's `.forge/` documents and freezes the sprint's context manifest via `ContextManifestCompiler.Compile` (itself previously zero-caller), then drives `SprintScheduler.StartAttemptAsync`/`CompleteAttemptAsync` (also both zero-caller before this slice) to a `Succeeded` `NodeResult` recording the manifest's own digest as `input_digest` and its admitted items' digests as `outputs`. This is the first code in this repository that mutates durable workflow state without a human command behind it, so it mirrors `ResumeSchedulerHostedService`'s exact "no per-sprint memory, re-derive everything from durable state every tick" shape and leans on `SprintScheduler`'s own idempotency for crash-resumability rather than inventing a second mechanism. A `.forge/` parse error is recorded as a `NodeDiagnostic` on the still-`Succeeded` node rather than failing it, matching `IntegrationGenerationService`'s existing precedent for the identical input and avoiding burning all automatic retries on a document a retry can never fix. The token budget `ContextManifestCompiler.Compile` requires has no configuration key anywhere yet, so a fixed `32_000` literal stands in, explicitly marked as an unverified MVP guess and debt, matching `SprintScheduler.DefaultRateLimitBackoff`'s own precedent for a frozen fallback with no configuration source. Explicitly deferred, and named honestly rather than implied closed: every model-bearing role (planning/implementation/review) and every other Work role (confirmation/test-work/finalization) still has no executor; `SprintGitIsolation`'s whole lifecycle remains zero-caller; `forge sprint rebase` and the two re-scoped snapshot fields remain blocked exactly as ADR 0027 left them, since this slice does not touch the model-bearing path they actually need; the token-budget configuration key; per-node context-manifest scoping (still deferred from ADR 0014); and durably persisting the manifest itself (only its digests reach durable state). **A first review round found and fixed three issues** in already-shipped `FileSprintEventLog` code this slice's first real callers exposed: `GetNodeResultsAsync` did not normalize a corrupt result file into `InvalidDataException` the way `LoadAsync`/`LoadDefinitionAsync` already did, so a corrupt file permanently faulted `IntakeExecutionHostedService`'s `ExecuteTask` with nothing observing it -- fixed at the root, benefiting every `ISprintStore` caller; budget-truncated context items were computed but silently discarded instead of recorded as diagnostics -- fixed with a new `context_item_truncated` diagnostic code; and the ADR's "reproducible by construction" justification for not persisting the manifest was unsound, since the executor parses the live `.forge/` tree while pinning only the code baseline -- corrected. **A second review round found the first round's `GetNodeResultsAsync` fix was incomplete** (a syntactically valid file with an explicit `"outputs": null`/`"diagnostics": null` still threw an unguarded `ArgumentNullException`, fixed by honestly typing those fields nullable and coalescing to empty) **and that the same unnormalized-corrupt-file gap existed one call further out**, in `GetConfirmationsAsync`/`GetFindingsAsync` (reachable from `AdvanceGraphAsync`/`CompleteAttemptAsync`, latent only because no executor can yet drive a sprint far enough to reach a real corrupt file there) -- fixed with the identical normalization pattern, restoring the service's own "nothing this method reaches is outside this boundary" claim to true. **A third (final full-scope) review round found the same defect class recurring a third time**: round 2's null-list fix covered `PersistedNodeResult` but not the structurally identical `PersistedConfirmation.Evidence`, which threw the same unguarded `ArgumentNullException` -- fixed the same way, with `PersistedFinding.Evidence` independently confirmed unaffected (no `.Select` to throw on). It also found a fourth, previously-untouched failure-boundary gap: `CompleteAttemptAsync`'s own routing-budget refund reaches `LoadValidatedEventsAsync`, which caught only `JsonException` (narrower than `LoadAsync`'s own filter), and its `MigrateLegacyRoutingAsync` one-time pre-v0.11 sidecar migration could raise unguarded `KeyNotFoundException`/`ArgumentNullException` on a damaged legacy file -- fixed by widening the catch to match. After three rounds, five instances of the same "unwrapped JSON/parse exception escapes a per-sprint catch filter" defect class were found and fixed across `FileSprintEventLog`; round 3 traced every `ISprintStore` read reachable from this service's own call chain and found none remaining unnormalized (`GetHandoffsAsync`/`GetReviewIterationsAsync` share the historical gap but are genuinely unreachable from here, left out of scope). **A fourth (critical-only) round found a sixth instance**, this time inside code round 1 itself had added: `GetNodeResultsAsync`'s own `Guid.Parse(persisted.AttemptId)` was never covered by the widened filter, the exact `ArgumentNullException` hazard round 3's own fix comment for a sibling method already named verbatim -- fixed by widening this method's own catch filter, since (unlike `Outputs`/`Diagnostics`/`Evidence`) a null attempt id is corrupt data, not a legitimate empty value to coalesce. Six instances of the identical defect class across four rounds is itself the honest finding: ADR 0028 names this as whack-a-mole and records a shared deserialization-validation helper for `FileSprintEventLog`'s `Persisted*` DTOs as deferred cleanup, out of this PR's own scope. **A fifth (critical-only) round found two more instances of the same defect class in a different shape**: round 2/3's `?? []` list-level coalesces guarded a null list, not a null element inside a present one (`"diagnostics": [null]`/`"evidence": [null]`), and the null element's own dereference in `FromPersisted` threw an uncaught `NullReferenceException` -- fixed by widening both catch filters. While closing those two, a ninth instance was self-identified before launching a sixth round: `LoadDefinitionAsync` (this service's own first read, ahead of the other two) had the identical null-list-element hazard across its `graph`/`dependencies`/`execution_profiles` arrays -- fixed the same way. Nine instances across five rounds upgrades ADR 0028's deferred shared-validation-helper item from "worth doing eventually" to an explicit trigger: a tenth instance found by a sixth round should prompt that redesign rather than a tenth narrow patch. **A sixth (critical-only) round found the tenth instance**, and reconsidered rather than silently followed the round-5 commitment to escalate: `LoadDefinitionAsync`'s own `graph`/`dependencies`/`execution_profiles` had the null-*list* variant of round 5's null-*element* fix for the same method. Fixed narrowly again -- but not uniformly, since the trace surfaced a real semantic split the mechanical `?? []` pattern would have gotten wrong: `dependencies`/`execution_profiles`/a node's own `depends_on` coalesce to empty (all legitimate empty values), while `graph` is deliberately left to throw, since an empty graph trivially passes `SprintGraphValidator.IsValid` and would silently produce a frozen sprint with zero executable nodes -- corrupt data masquerading as valid rather than failing loudly. The systematic redesign (a `RespectNullableAnnotations` read-path or a shared validating deserializer) was reconsidered and deferred again, honestly: it is only as correct as every `Persisted*` type's own field nullability, which nothing has yet audited end to end, and that audit is real design work deserving its own slice, not one folded into round 6 of an already six-round PR. **A seventh (critical-only) round found the eleventh instance, which broke the pattern the first ten shared**: `SprintScheduler.StartAttemptAsync`'s own `running`-node resume path parses `NodeSnapshot.CurrentAttemptId` -- a free-form event-journal argument, never a `Persisted*` DTO field -- so no `FileSprintEventLog` redesign, however systematic, could ever have caught it. This settled the question the prior three rounds kept re-litigating: the eleven instances share no single root cause narrow enough to fix at its source, but they do share the one place they all become observable, `IntakeExecutionHostedService`'s own per-sprint catch filter, which round 7 widened to catch every exception type any of the eleven instances has actually produced. ADR 0028's deferred `FileSprintEventLog`-level fix is downgraded accordingly, from a correctness backstop to a diagnosability-only improvement.
- [x] P11.67–P11.72 — Add best-effort local notifications for `awaiting_human`, `blocked`, `failed`, and `completed`, deduplicated from journal event ids and redacted. **Closed 2026-08-18:** ADR 0024 adds the delivery half of a projection pipeline mostly already built and tested under an earlier stage (`NotificationProjector`, whose own doc comment named "actual delivery... a platform-owned concern for a later stage to add" — this is that stage). Dedup reuses `ControlEventsReader`'s own `ControlEventsCursor` mechanism directly (`forge events --cursor`'s exact durable, gap-safe resume guarantee) rather than inventing a separate delivered-event-id set — `NotificationDeliveryCursorStore` persists one opaque cursor token per project at `.forge/notifications/cursor.json`. `NotificationDeliveryHostedService` (`Forge.Host.Runtime`) mirrors `ResumeSchedulerHostedService`'s exact lifecycle shape (a `PeriodicTimer` sweep, lease-scoped start/stop via `ControlPlaneHostedService`, per-item failure isolation so one bad delivery or one corrupt cursor never faults the whole service), gated by a new `notifications.enabled` configuration key (User scope, default on, satisfying ADR 0005's "user-configurable") — while disabled, the cursor still advances so re-enabling later never delivers a backlog of stale historical notifications as a burst. The notification body is redacted via the existing `SecretRedactor` even though today's fixed "Sprint: {id}" body never actually contains a secret, a defensive, forward-compatible choice rather than an urgent one. `INotificationService` is a new neutral port in `Forge.Runtime` (ADR 0007's "notification policy... is cross-platform; OS notification delivery" is the one adapter-owned piece), defaulting to a silent no-op (`NullNotificationService`) until a platform composition root overrides it — mirroring `IPlatformPreflight`'s own default-then-override pattern exactly. `WindowsNotificationService` (`Forge.Runtime.Windows`) delivers via `System.Windows.Forms.NotifyIcon.ShowBalloonTip` rather than the modern Windows App SDK notification API: Windows 10/11 already render a balloon-tip call as a standard Action Center toast, and this needs neither a message pump nor an unpackaged-app AUMID registration step (both of which the App SDK API requires, built for a primary UI application, not Forge Host's headless background process) nor any new NuGet dependency at all — `System.Windows.Forms` is a framework reference the project's TFM already carries. Honestly named, not silently assumed away: this specific piece (does a balloon tip actually render) cannot be verified by this repository's own test suite or by an independent review agent, since no interactive desktop session exists in CI; every other piece of the feature (cursor persistence/dedup, config gating without a backlog burst on re-enable, redaction, per-failure isolation) is covered by six new `NotificationDeliveryHostedServiceTests` against a fake `INotificationService`. A missing registration in `ControlPlaneTests`'s own in-test Host builder (which manually mirrors `ForgeHostApplication.RunAsync`'s DI setup rather than reusing it) was caught immediately by the full suite run once `ControlPlaneHostedService` started requiring the new service in its constructor — fixed by adding the same `NotificationDeliveryOptions`/`NotificationDeliveryHostedService` registrations the production composition root already has. Repeated full-suite local validation runs also surfaced intermittent, previously-nonexistent flakiness in unrelated `ControlPlaneTests` round-trip tests (roughly 2 of 5 runs) traced to resource contention: every real test `ControlPlaneHost.StartAsync` now also starts this new service, whose `ResumeSchedulerHostedService`-mirrored immediate-first-tick design meant every Host start synchronously did an extra durable-event read plus cursor file write at the exact moment of peak concurrent Host-startup load. Fixed by deferring this service's own first sweep until its first `PollInterval` elapses rather than ticking immediately — a best-effort notification has no correctness reason to fire the instant a Host starts, unlike `ResumeSchedulerHostedService`'s own immediate tick, which stays unchanged since promptly unsticking a node is a real correctness concern. Four full-suite runs after the fix were clean. **An independent review round found five issues, most seriously that stale-cursor recovery replayed the project's full history**: the naive fresh-anchor cursor has empty watermarks, which does not itself skip already-delivered events, it only makes them look unseen again — fixed with `CatchUpToNowAsync`, which reads forward to the journal's actual current tip before persisting, verified with a regression test proven to fail against the pre-fix code. The same round found `docs/contracts/v1/configuration.json` was never updated for the new key (fixed, plus a new cross-check test so this specific drift cannot recur silently), `NotificationDeliveryCursorStore.SaveAsync` leaked a `.tmp` file on failure (fixed with cleanup-then-rethrow), settings-reading sat outside this service's own tick-level failure isolation and separately ran the Host's entire startup pipeline just for a language tag (fixed by reading it from the same config view already used for the enabled check, inside the existing try/catch), and two of the six original tests proved less than their names claimed (the delivery-failure test used one event and only checked `File.Exists`; rewritten with two sprints proving genuine per-item isolation and real watermark advancement). **A second review round found round 1's own settings-isolation fix had reintroduced round 1's own bug**: `CatchUpToNowAsync`, added in that same round-1 commit, ran outside `TickAsync`'s try/catch, so a transient failure during stale-cursor recovery (up to 1,000 unguarded reads) would have permanently faulted `ExecuteTask` unlogged. Fixed by restructuring the whole tick into one try/catch matching `ResumeSchedulerHostedService`'s own shape. The same round found the catch-up loop's own stopping condition compared *deliverable* event counts, undercounting whenever `ReadControlEvents` withholds a gap-stranded event and ending catch-up early with history still unread — fixed by terminating on cursor-value stability instead. Three further fixes: an unreadable configuration was treated as "enabled" (could silently override a genuine `notifications.enabled=false`), fixed to fail closed; the round-1 `.tmp`-cleanup fix could itself throw and replace the original exception, fixed with a nested try/catch; and two tests were strengthened (`sensitive` added to the contract-consistency check; the disabled-cursor test now compares against an independently-computed ground-truth watermark instead of only asserting non-negativity). **A third review round found the same "does the try/catch actually cover everything reachable from `TickAsync`" defect class a third time**: `InvalidDataException` (thrown for a corrupt journal, reached via `ControlEventsReader.ReadAsync` the same way `ResumeSchedulerHostedService` already handles it) still escaped the catch filter despite this service's own doc comment already claiming parity with that shape — fixed by adding it, with a regression test that corrupts a sprint's own event log and asserts the service stays unfaulted. The same round found the fail-closed config-read path (round 2's own fix) logged nothing, making a transient failure indistinguishable from a deliberate `notifications.enabled=false` in the logs alone — fixed with a warning log plus an end-to-end test; found the delivery-failure-isolation test still used a bare `watermark >= 0` assertion, the exact pattern round 2 rejected in the sibling disabled-cursor test — fixed with the same ground-truth comparison, extracted into a shared helper; found `NotificationDeliveryCursorStore.SaveAsync`'s own cleanup path, rewritten by both prior review rounds, had zero tests — fixed with a new unit test forcing the final `File.Move` to fail against a real leftover temp file (surfacing `UnauthorizedAccessException` on this Windows environment, not the `IOException` originally assumed — already covered by the same catch filter either way); and found the catch-up bound's doc comment and log message both inaccurately described unresolved events as later being "caught up on" rather than delivered as ordinary new notifications — reworded for accuracy. **A fourth, critical-only review round** (round 3 was the last full-scope round per AGENTS.md) found the round-3 cursor-cleanup test itself broke the portable CI job on Linux and macOS: it asserted an exact `UnauthorizedAccessException` type, confirmed only on Windows, but moving a file onto an existing directory raises `IOException` (`EISDIR`) instead on those platforms — confirmed from the actual CI failure log, not inferred. Fixed by asserting the thrown exception is one of the two types the hosted service's own catch filter already accepts, rather than one exact type. The same round re-verified from scratch that every path reachable from `TickAsync` is covered by its try/catch and found no fourth instance of that recurring defect class. **A fifth review round**, after confirming the round-4 fix directly against CI (the exact ubuntu-24.04/macos-14 jobs that had failed now passed), found one further critical issue of the same shape — a test reliable on a dev box but not in CI: `ADeliveryFailureIsIsolatedAndTheCursorStillAdvancesPastBothEvents` stopped the service as soon as delivery was observed, a point strictly before `TickAsync` reaches its own cursor save, so a cancellation landing inside that save could leave `cursor.json` never written — reproduced directly, not theorized. Fixed with a `WaitForCursorAsync` helper that polls the persisted cursor itself until both sprints' watermarks are present before stopping. **A sixth review round** — a sixth instance of the "passes locally, not reliably under CI-shaped load" pattern, this time in round 5's own new helper — found `WaitForCursorAsync` was the only cursor read in the file running while the service was still ticking, so `SaveCursorAsync`'s own every-tick `File.Move(overwrite: true)` could occasionally race its unguarded read and throw `IOException` (reproduced empirically: 0.075% per read under low contention, 2.9% under CI-shaped heavy contention). Fixed by wrapping the read in the identical `IOException or UnauthorizedAccessException` tolerance `NotificationDeliveryCursorStore.LoadAsync` itself already uses for this same file. **A seventh review round** found three further critical issues, all one root cause: fixed time budgets (a 2-second/40-attempt poll, two 300ms delays) that CI-shaped contention (6.5x-20x local slowdown, reproduced directly) could exceed before even one real tick landed, producing failures indistinguishable from genuine defects. Fixed by replacing the fixed attempt count on both polling helpers with a shared 10-second wall-clock deadline, and replacing the two fixed-delay-then-read tests with `WaitForCursorAsync`-based polling for the ground-truth watermark. **An eighth review round**, reproducing load at far greater scale (352 samples, up to 48-way process contention plus 240 CPU-burning threads on a 24-core box), found even the 10-second deadline insufficient in 9/352 samples of the two-sprint delivery-isolation test — failure was not monotonic in slowdown (one run failed at 26s while another in the same wave passed at 65s against a 1s baseline), a scheduling coin flip once oversubscribed rather than a margin that scales predictably, so no fixed deadline is provably enough in principle; product code was not implicated in any sample. Widened the shared deadline to 30 seconds, which costs nothing on the happy path. Explicitly deferred, matching this item's own honest scope: click-through activation (no argument routing back into Forge), a Desktop-side notification history or in-app "attention" view (the still-open Stage 11 P11.56-P11.66 navigation shell is the more natural future home), and network notification channels/custom scripts (ADR 0005 already named both outside the MVP).

**Gate:** both surfaces expose equivalent commands and snapshot projections; prompts/environment/output/processes are bounded; nodes inherit neither ambient context nor authority; process exit cannot self-certify completion; implementation confirmation precedes new test selection and authoring, test expectations remain independent from implementation, and fixes retain proven regression tests; review follows ADRs 0006 and 0008, always runs fresh, and treats provider/model independence as best-effort; rate-limit, supersession, notification, and crash recovery are durable.

## Stage 12 — Diagnostics, evaluations, and hardening

- [ ] P12.1–P12.8 — Add safe OpenTelemetry traces/metrics, structured logs, and allowlisted `forge doctor --bundle`; omit source/diffs, prompts, provider output, raw commands, credentials, full environments, and unredacted personal paths.
- [ ] P12.9–P12.15 — Add updater/provider/bootstrap/workflow evaluations and model-policy gates that run through existing commands; keep evaluation orchestration out of presentation code.
- [ ] P12.16–P12.32 — Test release trust, injection, IPC identity, protocol confusion, environment leakage, stdin/output denial of service, orphan processes, notification disclosure, permissions, dependencies, licenses, accessibility, localization, parity, migration, routing fold, provider selection, cached conditional updates, authentication gates, disabled-provider isolation, provider-view testing-order parity, Host rejection of premature test work, review convergence, same-lineage review fallback, and supersession.

**Gate:** no critical finding remains; diagnostic negative tests pass; safety, parity, accessibility, localization, compatibility, observability, and release-trust thresholds pass.

## Stage 13 — MVP release and acceptance

- [ ] P13.1–P13.10 — Produce reproducible Windows x64/Arm64 bundles containing neutral Host/CLI/presentation code and provider-owned thin Windows adapters; publish matching version, checksums, SBOM, annotated tag, and release notes.
- [ ] P13.11–P13.24 — Run clean-profile install/update/rollback and end-to-end workflow acceptance: provider enablement, conditional updates, authentication, project init, concurrent sprints, fallback, client restart/update, single writer, snapshot/events, human gates, supervised execution, implementation-first testing enforcement, best-effort lineage review, deferral, supersession, review convergence, notifications, diagnostics, localization, and release/development isolation.

**Final gate:** every stage is complete, the signed Windows MVP is reproducible, architecture matches implementation, CI is green, and no blocking/high finding remains.

## Post-MVP learning backlog

- [ ] Derive an evidence-backed `KnowledgeProposal` only from a successful sprint, a corrected failure, or explicit operator instruction. Record applicability, canonical `.forge/` target, source event/artifact references, and a reviewable diff.
- [ ] Reuse the existing human-gate contract to inspect, approve, or reject each proposal. Approval updates canonical `.forge/` content and then regenerates owned provider views; agents cannot approve, directly edit generated skills, or learn from raw transcripts/provider prose.
- [ ] Evaluate an optional per-user hot-memory snapshot for personal preferences and verified environment facts. Keep it separate from project scope, hard-capped, frozen at attempt start, injection-scanned, credential-free, and explicit on overflow; never store project truth in it.
- [ ] Retrieve project knowledge from canonical files and content-addressed sprint artifacts. Add full-text or semantic recall only after an evaluation proves exact lookup insufficient and freshness against the source commit is enforceable.
- [ ] Ship learning only after evaluations demonstrate better task quality without regressions in reproducibility, prompt-injection resistance, approval integrity, privacy, or context cost; include proposal provenance, rollback, migration, and expiry tests.

## Intentionally not planned

- Model-authored executable tool pipelines; declarative context-query plans cover the useful case.
- Shadow Git checkpoints; isolated attempt worktrees and explicit supersession already provide rollback and replay.
- LLM goal judges or provider exit codes as completion authorities; deterministic workflow gates own completion.
- Conversation-history databases as project memory; transcripts are neither state nor canonical knowledge.
- Unrestricted lifecycle hooks or silent agent-managed skill writes; extensions and learned changes require validation, bounded authority, and explicit approval.

## Deferred

- Linux/macOS distribution and native Desktop hosts.
- `standard` and `fast` workflows.
- Full-text, Tree-sitter, LSP, graph, SCIP, or semantic indexes until measured need.
- Network notification channels and custom scripts.
- Distributed workers, SaaS, enterprise policy, package-manager, and machine-wide distribution.

## Evidence

| Stage | Canonical evidence |
|---:|---|
| 0 | ADR 0001; `docs/contracts/v1/`; Stage 0 contract gate |
| 1 | `Forge.slnx`; `src/`; `tests/Forge.Tests/`; `.github/scripts/test-stage1.ps1` |
| 2 | `src/Forge.Updater/`; updater tests |
| 3 | `src/Forge.Updater.Windows/`; Windows publish/installer tests |
| 4 | startup/configuration/initialization implementation and acceptance tests |
| 5 | ADR 0002; `src/Forge.Runtime/Providers/`; provider tests; PR #18 |
| 6 | ADR 0003; sprint journal/scheduler tests; PRs #19–20 |
| 7 | ADR 0004; Git/routing tests; PR #21 |
| 8 architecture | ADRs 0005, 0007, and 0008; project snapshot capability; Stage 8 gate above |
| 9 | ADRs 0009, 0010, and 0011; `docs/contracts/v1/schemas/forge-document.schema.json` and `generated-artifact.schema.json`; PRs #49 and #50, and the PR closing P9.17-24 |
| 10 | ADR 0012; `docs/contracts/v1/schemas/context-manifest.schema.json`, `context-query-plan.schema.json`, and `context-result-bundle.schema.json`; PR #52 |
| 11 architecture | ADRs 0006 and 0008; supervised execution/review gate above |
| 11 (P11.1-12) | ADR 0013; `docs/contracts/v1/schemas/confirmation-result.schema.json`; PR #53 |
| 11 (P11.13-20) | ADR 0014; `docs/contracts/v1/schemas/execution-profile.schema.json`; PR #54 |
| 11 (P11.21-31) | ADR 0015; `docs/contracts/v1/schemas/review-iteration.schema.json`; PR #55 |

## Resolved decisions

| ID | Decision | Resolution |
|---|---|---|
| D-005 | Provider installation/update strategy | ADR 0002 |
| D-006 | Host, local protocol, read-back, and process ownership | ADR 0005 |
| D-007 | Provider supervision and review convergence | ADR 0006 |
| D-008 | Cross-platform core and minimal OS adapters | ADR 0007 |
| D-009 | Modular provider ownership, runtime selection, maintenance, authentication, and review lineage fallback | ADR 0008 |
